# requirements.txt - Updated for Video Analysis API
# Core Computer Vision & Video Processing
opencv-python>=4.8.0
ultralytics>=8.0.0
easyocr>=1.7.0
Pillow>=9.0.0

# Audio Processing & Transcription
openai-whisper>=20231117
ffmpeg-python>=0.2.0

# NLP & Text Processing
transformers>=4.35.0
spacy>=3.7.0
# Download models: python -m spacy download en_core_web_sm

# PyTorch Ecosystem
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Data Processing & Utilities
pandas>=1.5.0
numpy>=1.24.0
scipy>=1.10.0
tqdm>=4.65.0

# API & Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0
python-magic>=0.4.27
python-magic-bin>=0.4.14  # Windows compatibility

# HTTP Client & File Handling
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Development & Utilities
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.7.0  # Better logging alternative
psutil>=5.9.0  # System monitoring

# Optional: Database & Caching (if you add later)
# sqlalchemy>=2.0.0
# redis>=5.0.0
# pymongo>=4.5.0

# =============================================================================
# INSTALLATION COMMANDS:
# =============================================================================

# 1. Create and activate environment:
# conda create -n vaa1 python=3.10
# conda activate vaa1

# 2. Install packages:
# pip install -r requirements.txt

# 3. Download spaCy models:
# python -m spacy download en_core_web_sm
# python -m spacy download en_core_web_md
# python -m spacy download en_core_web_lg

# 4. Install system dependencies (Windows):
# - Install FFmpeg and add to PATH
# - Or use: conda install ffmpeg

# =============================================================================
# VERIFIED CONFIGURATION:
# =============================================================================
# ✅ Python Version: 3.10.19
# ✅ Platform: Windows (Anaconda)
# ✅ All Dependencies Tested and Working
# ✅ Compatible with FastAPI + Next.js frontend
# ✅ Supports YOLOv8, EasyOCR, Whisper, spaCy